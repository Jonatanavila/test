class rectacgulo:
	def __init__(self,vertice1=punto(X,y),vertice2=punto(x,y),vertice3=punto(x,y),vertice4=punto(x,Y)):
            self.vertice1=vertice1
            self.vertice2=vertice2
            self.vertice3=vertice3
            self.vertice4=vertice4
       def altura(self,Altura):
            self.Altura=
       def base(self,Base):
            self.Base=      
       def perimetro(self,Perimetro):
            self.Perimetro= (self.Base * self.Altura)
